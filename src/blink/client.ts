import { createClient } from '@blinkdotnew/sdk'

export const blink = createClient({
  projectId: 'geo-optimization-ai-traffic-booster-platform-5rl6e6lu',
  authRequired: true
})

export default blink